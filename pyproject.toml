[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "enn583-support"
version = "0.1.0"
description = "Instructor-supplied support modules for ENN583"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["kitti_utils"]
package-dir = {"" = "support"}
